# 提示词 · 视觉自检(闭环第④步)

> 先转成图,再逐页挑毛病。先查溢出——最常见、最显眼。

## 前置:把 PPT 转成图(claude.ai 环境;脚本路径以当前 pptx 技能为准)

```bash
python <pptx-skill>/scripts/office/soffice.py --headless --convert-to pdf deck.pptx
rm -f slide-*.jpg
pdftoppm -jpeg -r 150 deck.pdf slide && ls -1 "$PWD"/slide-*.jpg
# 把打印出的绝对路径直接交给 view 工具逐页看
```

## 直接发给 Claude

「把这几页当 defect 来挑,逐页列出观众能看到的问题:

- 文字溢出 / 被截断(最优先查)
- 元素重叠(文字压形状、线穿字)
- 没对齐(列不齐、卡片不齐)
- 空白不均(一处挤、一处空)
- 边距不足(贴边 < 0.5 英寸)
- 低对比(浅字浅底、深图深底)
- 逻辑断层(这页和上下页接不上)
- 配色跑了(超出 `styles/soe-finance.md` 的三套配色)

跳过像素级、观众根本看不出的吹毛求疵。列完→改→只重渲染改过的页复核一次,不要无限循环。」
