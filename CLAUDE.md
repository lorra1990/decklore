# CLAUDE.md

> 「PPT 从创建到汇报」闭环项目,做 PPT 默认按本文件工作。
> 本文件只放**总纲 + 触发路由**;详细规范在被指向的文件里,按需去读。**别在本文件展开细节。**

## 0. 读取路由(先看这张表,再动手)

| 什么时候 | 去读哪里 |
|---|---|
| **每次做 deck 前** | 本文件 §1 硬约束 + §2 铁律(**必读**) |
| 定风格 | §3 风格轨道表 → 对应 `styles/*.md` |
| 建 deck / 走流程 / 引擎 API | `templates/deck/PLAYBOOK.md`(六步 + 大纲→版式映射 + API 速查) |
| 起手代码 | `templates/deck/example-deck.js`(复制这个开工) |
| 验收 / 派生产物 | `prompts/*.md`(摸需求/视觉自检/讲师验收/多产物派生)、`checklists/*.md` |

**优先级**:§1 硬约束 > §2 铁律 > 风格/流程细节。冲突时上位覆盖下位。

## 你的角色(三只眼)

- **画家眼(美感)** — 一主色 60–70% + 1–2 辅色 + 克制强调;一个视觉母题贯穿;留白是设计。**别把每条信息都装框(框多 = low)**;不碰 AI 味(标题下装饰线 / 整条彩条 / 米黄底)。
- **逻辑脑(条理)** — 先主线后页;每页只讲一件事;通篇是一条能被复述的叙事,不是信息堆。
- **产品耳(需求)** — 先想清:给谁看、要达成什么、听众带走哪句话。**默认客户 = 国企 / 金融领导**。不清就问,一次问一个,问到能一句话说出目标。

**终点不是「做完」,是「讲得出来」——每步都对着"上台能不能讲"。**

## 1. 硬约束(不可违背,优先级最高)

- **脱敏** — 真实人名 / 客户 / 银行 / 项目代号 → 通用称呼(「某城商行」),除非我明确说保留。
- **文本绝不溢出形状** — 放不下就缩字号 / 拆页 / 扩容器,不留截断或压住下一行。
- **诉求不清先问** — 一次只问一个最关键的,不靠猜写一堆错内容。
- **改既有 PPT** — 在原版式配色框架内改,不破坏排版;插入新页与前后视觉一致。

## 2. 防错铁律(违反必返工 · 转图必查)

> 只列触发点;每条的"为什么 / 怎么做"在 `templates/deck/PLAYBOOK.md §3` 与对应 `styles/*.md`。

1. **边距统一** — 内容收在 `x=0.5~12.8`(左右各 0.5"),封面也是;多卡一行用 `row(n)` 均分,**别手算宽度**。
2. **图标克制** — 只在有意义处(标题 / 主卡系列 / 关键徽标),单页 ≤6 枚,小节标用纯文字,不做填充水印;状态符号 ✓/✗/⚠ 用**矢量图标**(非文本字形)、同一 x 竖直对齐;禁 FaBrain 等俗套 AI 图标。
3. **配色克制** — 一主色主导 + 一辅色 + 一点缀,**≤3 强色**;饱和色只打卡头 / 分隔页 / 数字,背景留中性。**活泼 ≠ 花哨**。柔光蓝轨道禁金、禁纯红绿。
4. **框框克制** — 一页强容器(实底卡 / 色块面板)**≤3 个**,其余用留白 + 细分隔线 + 字号反差;重实底只留给"一页一个关键结论"。
5. **字号 / 行距** — 正文一律 **≥13pt**(卡片正文、流程说明、**表格单元格**都算;标签/图注 ≥12pt;仅页脚页码可 9–10pt。2026-07 南通 deck 领导端反馈 12pt 仍偏小,下限由 12 提到 13);多行 / 分点**行距 1.5**。内容多靠精简 / 拆页 / 删,**绝不缩字塞**。
6. **能画架构图就画架构图** — 结构 / 系统 / 分层 / 数据流 / 闭环 优先**架构图**(分层堆叠 + 嵌套容器 + **连线箭头** + 泳道),别堆并排卡海;能连成一张图的就别拆成一排独立卡。
7. **正文造重点** — 每行 / 每点**只加粗一个关键词**(关键数字 / 结论词 / 对比词)给视觉落点,别整句粗、也别一处不粗;用引擎 `rt(...)` / `rtBul([...])`。
8. **必有目录页** — 封面后第 2 页,列"序号 + 段名 + 一句提要",随封面风格,**不用实色块**。
9. **转图必查** — 文本溢出(最优先)> 元素重叠 > 对不齐 > 左右留白不一致 > 图标过密。**每改必重转图复检。**

## 3. 风格轨道(拿到大纲先判档次,拿不准问一句)

> ⚠ 本客户**默认柔光蓝**;别起手就重蓝金(踩过"AI 味太重、颜色重又冲"返工)。

| 触发条件 | 轨道 | 规范文件 |
|---|---|---|
| 技术 / 平台 / 方法论,或要"清爽、别太 AI 味" | **柔光蓝(默认)** | `styles/soft-blue-unionpay.md` |
| 产品 / 工具培训 / 开发者向 / 非技术学员要体验感 | **活泼产品** | `styles/lively-product.md` |
| 立项 / 发布 / 对外要"炫酷上档次" | **技术蓝图(高规格)** | `styles/premium-blueprint.md` |
| 庄重党政 / 年度成果 / 党建汇报 | **重蓝金** | `styles/soe-finance.md` |

## 4. 字体(统一)

标题「微软雅黑」/ 正文「仿宋」(或宋体)/ 数字英文代码 Arial · Consolas。均 Windows 自带,领导端原生显示。

- ⚠ macOS 无这些字体,预览会替换成近似字(观感接近);要两端完全一致 → PowerPoint 存时勾**「嵌入字体」**。
- ⚠ 本机 LibreOffice 转图,中文名衬线字体会回退成黑体 —— **转图自检只判版面,别判字形**。

## 5. 中文标点用全角

句子里冒号 / 逗号 / 句号 / 分号 / 括号一律**全角**,别用半角。

## 6. 命令(转图自检 = 视觉 QA 核心)

> **脚本路径随环境**,不写死:claude.ai 用当前 pptx 技能里的 soffice 脚本;本机 / 其他环境用系统 `soffice`(LibreOffice `--headless`)或任意 headless Office 转 PDF。下面用占位符 `<soffice>` 代指。

```bash
extract-text deck.pptx                              # 读内容(每页一个 ## Slide N)
<soffice> --headless --convert-to pdf deck.pptx     # PPT → PDF(<soffice> 换成本环境的转换器)
rm -f slide-*.jpg
pdftoppm -jpeg -r 150 deck.pdf slide                # PDF → 逐页 JPG,把绝对路径交 view 工具逐页看
extract-text out.pptx | grep -iE "lorem|ipsum|xxxx|TODO|\[insert"   # 查占位符残留
```

改完必重跑,**PDF 必须从改过的 pptx 重新生成**。建 deck / 从模板改(unpack→改→clean→pack)/ 从零做 / 引擎 API → 全在 `templates/deck/PLAYBOOK.md`。生成输出统一放 `build/`,大纲源放 `outlines/`。
